type Query {
  hello: String
  getUser: GetUserResponse
}

type Mutation {
  saySomething(something: String): String
}

type User {
  name: String
  age: Int
}

type GetUserResponse {
  ok: Boolean
  user: User
  error: String
}
